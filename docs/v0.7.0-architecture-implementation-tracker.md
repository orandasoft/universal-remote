# Universal Remote v0.7.0 Architecture Implementation Tracker

## Purpose

This file provides a durable implementation and documentation handoff for the
v0.7.0 protocol and profile architecture work.

Update it whenever implementation findings affect requirements, architecture,
testing guidance, or explanatory documentation.

## Baseline

- Requirements document: `Universal Remote - Protocol and Profile Architecture - Revision 5`
- Baseline release: `v0.6.1`
- Baseline commit: `0c5e593`
- Working branch: `feature/v0.7.0-protocol-profile-architecture`
- Primary objective: preserve v0.6.1 behavior while introducing the protocol
  and profile architecture described by Revision 5.

## Completed implementation checkpoints

| Commit | Change | Requirement coverage |
|---|---|---|
| `35667dd` | Add LG TV Japan command identity regression tests | `PR-002`, `CS-007`, `T-004`, `Q-001`, `Q-002` |
| `4ebe240` | Convert `protocols.py` into the `protocols` package | Structural groundwork for `PR-003`, `PR-010`, `Q-001`, `Q-007` |
| `ee89b95` | Extract the protocol-neutral command model into `protocols/base.py` | `PR-005`, `Q-001`, `Q-007` |
| `e447a41` | Extract NEC-family implementation into `protocols/nec.py` | `PR-002`, `PR-010`, `PR-015`, `Q-007` |
| `df0333d` | Add protocol registry contracts and validation | Foundations for `PR-003`, `PR-004`, `PR-013`, `T-007`, `AC-002` |
| `22b0553` | Register the NEC and NEC1-F16 receive handlers | `PR-003`, `PR-004`, `PR-007`, `PR-009`, `PR-013`, `AC-002` |
| `ca644a3` | Move repeat decoding into protocol handlers | `PR-008`, `PR-015` |
| `724057d` | Pin Ruff and verify formatting in CI | `Q-002` reproducibility |
| `d2effa7` | Migrate receiver-event decoding and matching to the shared registry | `PR-003`, `PR-004`, `PR-006`, `PR-008`–`PR-010`, `PR-012`, `PR-013`, `PR-015`, `T-001`, `AC-002` |
| `061a2ee` | Migrate learning and decoder selection to the shared registry | `PR-003`, `PR-004`, `PR-007`, `PR-010`, `PR-012`, `PR-013`, `PR-016`, `T-001`, `AC-002` |
| `2eefb48` | Add immutable device-profile contracts, generic and TV profiles, and deterministic registry validation | Revision 5 profile-architecture groundwork; `Q-001`, `Q-002`, `Q-007` |
| `177cc10` | Resolve media-player entity eligibility, roles, sources, command UI, and diagnostics from profiles | Revision 5 profile-driven entity behavior; `Q-001`, `Q-002`, `Q-007` |
| `d564da9` | Add generic capability contracts, Japanese tuner semantics, capability registration, validation, and isolated resolver tests | Revision 5 capability-architecture groundwork; `Q-001`, `Q-002`, `Q-007` |
| `c5d97a0` | Add immutable codeset definitions, validated profile, decoder-family, and capability bindings, and extract dependency-free command-name resolution | Revision 5 codeset-binding architecture; `Q-001`, `Q-002`, `Q-003`, `Q-007` |
| `9d97cbb` | Add setup-time profile, codeset, and capability resolution and store the immutable semantic model in config-entry runtime data | Revision 5 resolved-profile architecture; `Q-001`, `Q-002`, `Q-003`, `Q-007` |
| `a007b48` | Make tuner runtime behavior capability-driven, inject resolved tuner semantics during setup, and remove hardcoded Japanese tuner routing from the runtime | Revision 5 capability-consumer architecture; `Q-001`, `Q-002`, `Q-003`, `Q-007` |
| `2f5c776` | Make command presentation capability-aware through the resolved profile and ordered capability model, remove the fixed Japanese tuner-prefix dependency from generic command UI classification, and pass resolved semantics into button setup | Revision 5 capability-consumer architecture; `Q-001`, `Q-002`, `Q-003`, `Q-007` |
| `1de6d45` | Align both user-facing READMEs with resolved codeset, profile, capability, tuner-runtime, command-presentation, receiver-event, and persisted import behavior | Documentation alignment; `DOC-001`, `Q-001` |
| `3c90b33` | Make device-type discovery, ordering, validation, and presentation profile-owned; prove a profile can exist without a codeset | `PF-007`, `PF-013`, `CS-002`, `CS-009`, `Q-003`, `Q-007` |
| `3aee123` | Document the future stateful-command boundary without introducing a speculative climate API | `PF-005`, `PF-014`, `UP-001`, `UP-005`, `Q-001` |
| `e5e31f9` | Resolve stable tuner IDs through configured selector aliases before transmission | `PF-006`, `PF-010`, `T-005`, `Q-001`, `Q-002` |
| `019cb4e` | Resolve decoder handlers, immutable match maps, and event types at config-entry setup | `PR-003`, `PR-004`, `PR-006`, `PR-013`, `CS-003`, `CS-009`, `Q-003`, `Q-004`, `T-009`, `AC-012` |
| `8769dd1` | Move repeat-association timing policy from generic event orchestration into receive protocol handlers | `PR-008`, `PR-010`, `PR-013`, `AC-001`, `Q-001`, `Q-002` |
| `2f34dbc` | Correct both READMEs to describe no-codeset and unmatched decoded receiver events accurately | `PR-011`, `PR-014`, `Q-001`, `DOC-006` |
| `51c540a` | Enforce 100% coverage and add Hassfest and HACS validation jobs | `Q-002`, `AC-009` |

## Current verification baseline

At commit `51c540a` (`Enforce release validation in CI`):

- The complete pytest suite passes.
- Integration coverage is 100%, enforced with `--cov-fail-under=100`.
- Ruff check passes.
- Ruff formatting validation passes with the pinned Ruff version.
- mypy passes across the integration and tests.
- Python compileall passes across the integration and tests.
- Git whitespace validation passes.
- The GitHub Actions `tests` job passes.
- The GitHub Actions `hassfest` job passes.
- The GitHub Actions `hacs` validation job passes.

The workflow now treats these checks as release gates rather than advisory
local verification.

## Current architecture state

### Protocol architecture

The protocol-neutral receive contracts live in:

    custom_components/universal_remote/protocols/base.py

Concrete NEC and NEC1-F16 behavior lives in:

    custom_components/universal_remote/protocols/nec.py

The explicit authoritative registry lives in:

    custom_components/universal_remote/protocols/registry.py

The registry supplies deterministic decoder-family ordering for receiver events
and learning. Received and codeset-generated commands use the same handler
normalization.

Protocol handlers own optional:

- receive decoding;
- normalized identity generation;
- repeat-frame decoding;
- repeat-association timing policy;
- learning confidence and labels;
- privacy-safe diagnostic metadata.

Generic `event.py` no longer contains an NEC-specific repeat-association
timeout. Registry validation rejects invalid repeat-policy declarations.

### Profile and device-type architecture

Immutable profile contracts live in:

    custom_components/universal_remote/profiles/base.py

The authoritative profile and capability registry lives in:

    custom_components/universal_remote/profiles/registry.py

Profiles own:

- stable profile and stored device-type IDs;
- deterministic device-type ordering;
- user-facing device-type labels;
- supported Home Assistant entity domains;
- ordered command roles;
- source rules;
- presentation overrides;
- optional capability references.

Device-type discovery and validation no longer depend on the presence of a
codeset. A fake non-generic profile with no codeset proves that a future device
type can be configured independently of fixed command-library availability.

### Capability architecture

The Japanese tuner capability lives in:

    custom_components/universal_remote/profiles/capabilities/japanese_tuner.py

`TunerCapability` owns:

- tuner declarations and selector aliases;
- numeric ranges;
- tuner availability;
- tuner-aware numeric routing;
- tuner inference;
- sent and received state-update policy;
- tuner command presentation.

Stable tuner IDs may differ from configured selector command names. The select
entity exposes the stable ID while `UniversalRemoteRuntime.async_select_tuner()`
resolves and sends the configured selector alias. State changes occur only
after successful transmission.

The fake `FM` tuner with selector command `RADIO` verifies that the runtime and
select entity are not tied to Japanese command names.

### Codeset architecture

The immutable codeset registry lives in:

    custom_components/universal_remote/codesets.py

Codesets supply declarative setup-time bindings to:

- an `infrared-protocols` enum;
- a base profile;
- an optional decoder family;
- ordered capabilities.

The `lg_tv_jp` codeset supplies the television profile and the
`japanese_tuner` capability. Other television codesets and manually configured
televisions do not gain Japanese tuner behavior from command names alone.

Codesets do not determine whether a device type exists and are not active
runtime processors.

### Setup-time resolved models

Semantic profile resolution and receiver resolution live in:

    custom_components/universal_remote/resolved.py

The per-entry aggregate is:

    UniversalRemoteData
    ├── resolved_profile
    ├── resolved_receiver
    └── runtime

`ResolvedRemoteProfile` owns immutable Home Assistant semantics:

- base profile;
- retained codeset definition, when compatible;
- deterministic ordered capabilities;
- merged command presentation.

`ResolvedReceiverModel` owns immutable receive bindings:

- decoder-family identity;
- ordered receive handlers;
- codeset-generated normalized match maps;
- supported receiver event types.

`UniversalRemoteRuntime` owns operational sending data and mutable per-entry
state:

- infrared emitter identity;
- copied configured-command mapping;
- tuner capability;
- selected tuner;
- send synchronization.

Receiver-only entries retain resolved profile and receiver models without
creating a sending runtime.

Normal signal handling no longer:

- resolves a decoder family through the codeset registry;
- loads an infrared-library enum;
- rebuilds codeset match maps;
- derives receiver event types.

Those operations occur during config-entry setup.

### Runtime boundaries

The outbound fixed-command path remains:

    Home Assistant entity action
    -> profile or capability resolves a named command
    -> configured stored payload
    -> protocol-neutral infrared emitter send

After payload resolution, sending does not consult receive handlers or codeset
enums.

The inbound named-command path remains:

    received signal
    -> resolved receive handler
    -> normalized protocol identity
    -> immutable codeset match map
    -> optional named command
    -> profile and capability semantics
    -> Home Assistant event and assumed-state update

Profiles do not inspect raw timings or protocol identity fields. Receive
handlers do not parse stored payloads or send commands.

### Receiver-event behavior

Receiver behavior is now documented consistently:

- no supported codeset: `unknown` with timing metadata;
- selected decoder family recognizes an unmatched command: protocol event such
  as `nec` or `nec1_f16`;
- normalized identity matches a codeset member: friendly named event such as
  `power` or `volume_up`;
- malformed or unsupported signal: `unknown`.

The documentation correction did not change runtime behavior.

### Future stateful-command boundary

v0.7.0 intentionally implements discrete named-command remotes.

Some devices, especially climate devices, encode complete mutable state in each
infrared frame. Future support should add a parallel stateful command model or
adapter that can:

- construct an `infrared-protocols` command from semantic device state;
- decode an incoming stateful command into semantic device state;
- describe supported state fields;
- remain independent of Home Assistant entity instances.

That future layer can reuse the completed profile registry, device-type catalog,
capabilities, resolved setup model, and infrared emitter/receiver boundaries.
It must not represent every state combination as a large fixed command map.

No speculative climate API is included in v0.7.0.

### Quality and release validation

The GitHub Actions workflow now enforces:

- Python compilation;
- Ruff linting;
- Ruff formatting;
- mypy;
- the complete pytest suite;
- 100% integration coverage;
- Hassfest;
- HACS repository validation.

All three CI jobs are currently passing.

### Compatibility preserved

The architecture work has not intentionally changed:

- stored config-entry data;
- stored codeset and device-type IDs;
- persisted command structures;
- imported Pronto payloads;
- entity unique IDs;
- device associations;
- remote and button service behavior;
- media-player role and source behavior;
- existing receiver-event payloads;
- existing Japanese tuner behavior.

### Remaining v0.7.0 work

The main architecture findings are closed.

Remaining release work is limited to:

- making diagnostics `source_count` profile-aware;
- adding the explicit outbound-isolation test required by `T-009`;
- completing live Home Assistant testing;
- performing the final compatibility and release-readiness audit;
- updating the manifest version and preparing release notes only after those
  checks pass.

## Documentation update register

| ID | Finding | Classification | Planned update | Status |
|---|---|---|---|---|
| `DOC-001` | Imported infrared-library commands are converted to Pronto and persisted. Updating `infrared-protocols` does not automatically regenerate already persisted commands. | Clarification | Documented in both user-facing READMEs, including intentional re-import behavior. | Completed |
| `DOC-002` | A proposed upstream `infrared-protocols` Pronto change may adjust carrier or timing conversion if it is merged and later adopted. | External future consideration | Not present in Universal Remote's current Pronto path and not part of v0.7.0. Reassess only after an upstream merge and dependency update. | Deferred |
| `DOC-003` | Testing guidance may need revision if a future upstream Pronto implementation changes waveform conversion behavior. | External future consideration | No current Universal Remote behavior requires this documentation. Reassess together with `DOC-002` after an applicable dependency update. | Deferred |
| `DOC-004` | `LGTVCodeJP.AMAZON` and `LGTVCodeJP.NETFLIX` generate NEC address `0xFB04` with `infrared-protocols` 8.2.0. | Verification only | Retain as implementation evidence for `PR-002`, `CS-007`, and `T-004`; no requirement change. | Verified |
| `DOC-005` | A Home Assistant installation previously appeared to contain an Amazon command using old address `0xFE04`. | Separate investigation | This is not currently reproducible as a v0.7.0 code issue. Investigate persisted data and the affected installation separately if it remains observable. | Deferred outside v0.7.0 |
| `DOC-006` | The READMEs incorrectly stated that a receiver without a codeset could emit decoded NEC-family protocol events. | Correction | Both READMEs now state that no-codeset signals emit `unknown`; protocol events require the selected codeset's decoder family. | Completed |
| `DOC-007` | Future climate support requires stateful command generation rather than a large fixed named-command map. | Architecture boundary | The tracker records a future parallel stateful-command adapter while keeping v0.7.0 focused on discrete named commands. | Completed |

## Requirement progress

### Protocol requirements

Implemented and verified:

- one authoritative receive and learning protocol registry;
- deterministic decoder-family ordering;
- protocol-neutral normalized identities;
- shared received/generated normalization;
- handler-owned learning metadata;
- handler-owned repeat decoding and association policy;
- handler-owned diagnostic metadata;
- protocol-neutral event and learning orchestration;
- receive handlers isolated from transmission.

### Profile and capability requirements

Implemented and verified:

- immutable generic and television profiles;
- profile-owned device-type discovery and validation;
- profile-driven entity eligibility, roles, sources, and presentation;
- composable immutable capabilities;
- Japanese tuner semantics isolated in `TunerCapability`;
- stable tuner IDs resolved through configured selector aliases;
- capability-driven sent and received state policy;
- manual profile semantics without a codeset;
- no raw protocol interpretation inside profiles.

### Codeset and setup requirements

Implemented and verified:

- one immutable validated codeset registry;
- stable existing codeset IDs;
- validated profile, decoder-family, and capability references;
- setup-time semantic profile resolution;
- setup-time receiver handler and match-map resolution;
- immutable receiver event-type derivation;
- receiver-only entry support;
- unchanged persisted command payload behavior.

### Test and extension requirements

Implemented and verified:

- fake non-NEC event and learning handlers;
- fake profile and capability behavior;
- NEC and NEC1-F16 parity coverage;
- Japanese tuner parity and alias coverage;
- no-codeset learning candidates;
- profile-without-codeset configuration;
- registry validation;
- 100% integration coverage.

Still required:

- an explicit outbound-isolation test proving that receive registries and
  codeset enums are not consulted after payload resolution.

### Quality requirements

- `Q-001`: v0.6.1 compatibility remains covered by the regression suite.
- `Q-002`: the complete suite passes with 100% integration coverage; Ruff,
  formatting, mypy, compileall, Hassfest, and HACS validation pass.
- `Q-003`: protocol, profile, capability, codeset, and receiver resolution are
  explicit and deterministic.
- `Q-004`: immutable receiver match maps are built during setup rather than
  during signal processing.
- `Q-005`: diagnostics continue to redact persisted command payloads and raw
  timing history.
- `Q-007`: registries, resolvers, handlers, profiles, capabilities, and runtime
  behavior remain independently testable.
- `Q-008`: no config-entry migration was introduced for the architecture.

### CI enforcement

The workflow now contains three passing jobs:

- `tests`;
- `hassfest`;
- `hacs`.

The test job fails when integration coverage falls below 100%.

### Still pending

- Make diagnostics source counting profile-aware.
- Add explicit outbound-isolation coverage for `T-009`.
- Run the live Home Assistant test matrix.
- Complete the final release-readiness and migration-compatibility review.
- Update the manifest version and prepare release notes only after the final
  gates pass.
- Revisit stateful command generation in a later feature release.

## Next implementation slice

Make diagnostics source counting profile-aware.

The current diagnostics payload should not report television source semantics
for a profile that does not support a media-player entity merely because the
configured command names resemble television source commands.

The checkpoint should:

1. Resolve source counting from the already resolved profile.
2. Report `source_count: 0` when the profile does not support `media_player`.
3. Preserve existing television source counting.
4. Preserve receiver-only diagnostics.
5. Preserve payload redaction and privacy-safe counts.
6. Add a generic-profile regression case containing `HDMI_1`.
7. Add or retain a television-profile source-count case.
8. Run the focused diagnostics tests.
9. Run Ruff, formatting, mypy, and compileall.
10. Run the complete suite with `--cov-fail-under=100`.

Do not combine this checkpoint with the outbound-isolation test or live testing.

After this checkpoint, add the explicit `T-009` transmission-boundary test as a
separate test-only commit.

## New-chat handoff

Branch:

    feature/v0.7.0-protocol-profile-architecture

Latest implementation checkpoint before this tracker update:

    51c540a Enforce release validation in CI

Recently completed review corrections:

- `3c90b33` profile-owned device types;
- `3aee123` future stateful-command boundary;
- `e5e31f9` tuner selector aliases;
- `019cb4e` setup-time receiver bindings;
- `8769dd1` handler-owned repeat policy;
- `2f34dbc` receiver documentation correction;
- `51c540a` enforced release validation.

Current CI state:

- `tests` passes;
- `hassfest` passes;
- `hacs` passes;
- test coverage is enforced at 100%.

The protocol, profile, capability, codeset, device-type, setup-time receiver,
tuner-runtime, command-presentation, repeat-policy, and user-facing receiver
documentation architecture findings are closed.

Remaining sequence:

1. Make diagnostics `source_count` profile-aware.
2. Add explicit outbound-isolation coverage for `T-009`.
3. Run the live Home Assistant test matrix.
4. Perform the final code, compatibility, persistence, and migration audit.
5. Update the manifest to v0.7.0.
6. Prepare release notes and release only after all final gates pass.

Do not begin climate implementation in v0.7.0. Future climate support should use
a parallel stateful command adapter while reusing the profile and device-type
architecture.

Do not change the no-codeset receiver behavior during the remaining v0.7.0
work. The established behavior is `unknown` with timing metadata.
