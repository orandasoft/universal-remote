# Universal Remote v0.7.0 Architecture Implementation Tracker

## Purpose

This file provides a durable implementation and documentation handoff for the
v0.7.0 protocol and profile architecture work.

Update it whenever implementation findings affect requirements, architecture,
testing guidance, or explanatory documentation.

## Baseline

- Requirements document: `Universal Remote - Protocol and Profile Architecture - Revision 5`
- Baseline release: `v0.6.1`
- Baseline commit: `0c5e593`
- Working branch: `feature/v0.7.0-protocol-profile-architecture`
- Primary objective: preserve v0.6.1 behavior while introducing the protocol
  and profile architecture described by Revision 5.

## Completed implementation checkpoints

| Commit | Change | Requirement coverage |
|---|---|---|
| `35667dd` | Add LG TV Japan command identity regression tests | `PR-002`, `CS-007`, `T-004`, `Q-001`, `Q-002` |
| `4ebe240` | Convert `protocols.py` into the `protocols` package | Structural groundwork for `PR-003`, `PR-010`, `Q-001`, `Q-007` |
| `ee89b95` | Extract the protocol-neutral command model into `protocols/base.py` | `PR-005`, `Q-001`, `Q-007` |
| `e447a41` | Extract NEC-family implementation into `protocols/nec.py` | `PR-002`, `PR-010`, `PR-015`, `Q-007` |
| `df0333d` | Add protocol registry contracts and validation | Foundations for `PR-003`, `PR-004`, `PR-013`, `T-007`, `AC-002` |
| `22b0553` | Register the NEC and NEC1-F16 receive handlers | `PR-003`, `PR-004`, `PR-007`, `PR-009`, `PR-013`, `AC-002` |
| `ca644a3` | Move repeat decoding into protocol handlers | `PR-008`, `PR-015` |
| `724057d` | Pin Ruff and verify formatting in CI | `Q-002` reproducibility |
| `d2effa7` | Migrate receiver-event decoding and matching to the shared registry | `PR-003`, `PR-004`, `PR-006`, `PR-008`–`PR-010`, `PR-012`, `PR-013`, `PR-015`, `T-001`, `AC-002` |
| `061a2ee` | Migrate learning and decoder selection to the shared registry | `PR-003`, `PR-004`, `PR-007`, `PR-010`, `PR-012`, `PR-013`, `PR-016`, `T-001`, `AC-002` |
| `2eefb48` | Add immutable device-profile contracts, generic and TV profiles, and deterministic registry validation | Revision 5 profile-architecture groundwork; `Q-001`, `Q-002`, `Q-007` |
| `177cc10` | Resolve media-player entity eligibility, roles, sources, command UI, and diagnostics from profiles | Revision 5 profile-driven entity behavior; `Q-001`, `Q-002`, `Q-007` |
| `d564da9` | Add generic capability contracts, Japanese tuner semantics, capability registration, validation, and isolated resolver tests | Revision 5 capability-architecture groundwork; `Q-001`, `Q-002`, `Q-007` |
| `c5d97a0` | Add immutable codeset definitions, validated profile, decoder-family, and capability bindings, and extract dependency-free command-name resolution | Revision 5 codeset-binding architecture; `Q-001`, `Q-002`, `Q-003`, `Q-007` |

## Current verification baseline

At commit `c5d97a0`:

- 797 tests pass.
- Integration coverage is 100% with zero missing lines.
- Protocol-registry, profile-registry, capability, codeset-registry,
  infrared-library, event, config-flow, options-flow, diagnostics, runtime,
  select, media-player, and integration suites pass.
- Ruff check passes.
- Ruff format check passes.
- mypy passes.
- Python compileall passes.
- Git whitespace checks pass.

## Current architecture state

### Protocol architecture

The protocol-neutral receive-side contracts are located in:

    custom_components/universal_remote/protocols/base.py

The concrete NEC and NEC1-F16 implementation is located in:

    custom_components/universal_remote/protocols/nec.py

The authoritative protocol registry is located in:

    custom_components/universal_remote/protocols/registry.py

It supplies all receive and learning handlers and defines deterministic
decoder-family ordering. Receiver-event and learning orchestration consume the
same registry without concrete NEC-family decision logic.

### Profile architecture

The immutable profile contracts are located in:

    custom_components/universal_remote/profiles/base.py

They include:

- `DeviceProfile`;
- `CommandRole`;
- `SourceRule`;
- `CommandPresentation`;
- `ProfileCapability`;
- explicit entity-domain declarations;
- ordered stable capability IDs.

The production generic and TV profiles are located in:

    custom_components/universal_remote/profiles/generic.py
    custom_components/universal_remote/profiles/tv.py

The authoritative profile and capability registry is located in:

    custom_components/universal_remote/profiles/registry.py

It provides deterministic registration, lookup, structural validation, and
validation of capability references.

Profile role and source resolution is located in:

    custom_components/universal_remote/profiles/resolver.py

Media-player eligibility, roles, sources, command-UI source classification, and
diagnostics source counting are resolved from profile declarations.

### Capability architecture

The Japanese television tuner capability is located in:

    custom_components/universal_remote/profiles/capabilities/japanese_tuner.py

It owns:

- stable capability ID `japanese_tuner`;
- tuner order `DTV`, `BS`, `CS1`, `CS2`, `BS4K`, and `CS4K`;
- keypad numbers 1 through 12;
- selector alias resolution;
- tuner-specific numeric-command resolution;
- available-tuner detection;
- selected-tuner keypad routing;
- tuner implication from sent and received command names;
- explicit sent-success and received-match update policies.

The capability is globally registered but is not attached unconditionally to
the base TV profile.

### Codeset architecture

The authoritative codeset definitions and bindings are located in:

    custom_components/universal_remote/codesets.py

Each immutable codeset definition owns:

- stable codeset ID;
- user-facing label;
- infrared-library module and enum class;
- base profile ID;
- optional receive decoder-family ID;
- ordered capability IDs.

The codeset registry validates:

- duplicate codeset IDs;
- missing profile references;
- missing decoder-family references;
- missing capability references;
- duplicate capability IDs within a codeset.

Production registration order is explicit and deterministic.

All existing codesets retain the TV base profile. Existing receive-capable
codesets retain their decoder-family bindings. The regional
`japanese_tuner` capability is bound specifically to `lg_tv_jp`.

`infrared_library.py` remains the compatibility and payload-generation facade.
Existing callers continue using its helper API while metadata ownership has
moved to the codeset registry.

### Command-name dependency boundary

Protocol-neutral command-name normalization and configured-command lookup are
located in:

    custom_components/universal_remote/command_names.py

This extraction removes the dependency cycle between generic integration
helpers, infrared-library compatibility helpers, codeset definitions, and
profile and capability resolution.

`helpers.py` retains explicit compatibility re-exports, preserving existing
callers.

### Compatibility and remaining boundaries

Stored configuration continues to use the existing `device_type` and `codeset`
values.

Generated and persisted command payloads are unchanged. Codeset enum members
continue through the existing `to_command()` and Pronto conversion path.

`runtime.py` still owns the current operational tuner state and routing.
`select.py` still creates its tuner entity from `runtime.available_tuners`.
Those consumers have not yet been migrated to resolved capabilities.

The following areas remain pending:

- setup-time resolution of the selected profile, codeset, and capabilities;
- storing the resolved model in config-entry runtime data;
- migration of tuner runtime routing to the resolved tuner capability;
- capability-driven tuner-select creation;
- migration of remaining consumers to resolved semantics;
- removal of obsolete Japanese tuner decisions after parity is proven.

## Documentation update register

| ID | Finding | Classification | Planned update | Status |
|---|---|---|---|---|
| `DOC-001` | Imported infrared-library commands are converted to Pronto and persisted. Updating `infrared-protocols` does not automatically regenerate already persisted commands. | Clarification | Clarify codeset import persistence and dependency-upgrade behavior. | Pending |
| `DOC-002` | Pronto carrier-frequency and timing conversion is quantized. A 38,000 Hz source carrier may decode from Pronto as approximately 38,029 Hz. | Clarification | Add a note to the Pronto persistence and testing discussion. | Pending |
| `DOC-003` | Protocol identity should be asserted exactly during round-trip testing, while modulation and individual timings require tolerances. | Testing clarification | Add to protocol parity and round-trip testing guidance. | Pending |
| `DOC-004` | `LGTVCodeJP.AMAZON` and `LGTVCodeJP.NETFLIX` generate NEC address `0xFB04` with `infrared-protocols` 8.2.0. | Verification only | Retain as implementation evidence for `PR-002`, `CS-007`, and `T-004`; no requirement change. | Verified |
| `DOC-005` | A Home Assistant installation previously appeared to contain an Amazon command using old address `0xFE04`. | Open investigation | Determine whether this was a persisted pre-upgrade payload or a stale runtime dependency before documenting a conclusion. | Open |

## Requirement progress

### Implemented and verified in the protocol-registry slice

- One authoritative registry supplies receive and learning handlers.
- Decoder-family order is explicit and deterministic.
- Received and codeset-generated commands share handler normalization.
- Event and learning orchestration are protocol-neutral.
- NEC and NEC1-F16 behavior and command identity remain covered.
- Repeat handling, diagnostics, and learning metadata are handler-owned.

### Implemented and verified in the profile-registry slices

- Immutable generic profile contracts have been introduced.
- Generic and television profiles are explicitly registered.
- Profile lookup by ID and stored device type is deterministic.
- Media-player eligibility, roles, sources, and diagnostics are profile-driven.
- Duplicate and invalid profile definitions are rejected.
- Existing media-player behavior remains unchanged.

### Implemented and verified in the capability-contract slice

- A generic immutable capability contract has been introduced.
- Capability definitions are explicitly and deterministically registered.
- Duplicate capability IDs and missing references are rejected.
- Japanese tuner semantics are isolated in a tuner capability.
- Available-tuner detection and numeric routing preserve existing behavior.
- A fake non-TV capability works without registry decision-logic changes.
- Japanese tuner behavior is not attached to every TV profile.

### Implemented and verified in the codeset-registry slice

- Codeset metadata has an authoritative immutable registry.
- Codesets bind deterministically to profiles, decoder families, and
  capabilities.
- `lg_tv_jp` alone receives the regional `japanese_tuner` capability.
- Existing receive-capable codesets retain their decoder-family bindings.
- Invalid profile, decoder-family, and capability references are rejected.
- Registry mappings are immutable and registration order is inspectable.
- `infrared_library.py` preserves its existing public helper behavior.
- Command-name normalization and lookup now have a dependency-free module.
- Existing payload generation, receiving, persistence, and entity behavior are
  unchanged.

### Quality requirements

- `Q-001`: v0.6.1 behavior remains covered by the complete regression suite.
- `Q-002`: 797 tests pass with 100% integration coverage and all
  configured quality gates green.
- `Q-003`: protocol, profile, capability, and codeset registration and ordering
  do not depend on import-order side effects.
- `Q-007`: handlers, profiles, capabilities, codesets, registries, and
  resolvers are independently testable without a Home Assistant instance.

### Still pending

- Setup-time resolution of profile, codeset, and capabilities.
- Config-entry runtime storage of the resolved semantic model.
- Migration of tuner runtime behavior to the resolved capability.
- Capability-driven tuner-select creation.
- Migration of remaining runtime consumers to resolved semantics.
- Removal of legacy Japanese tuner constants and decision logic.
- Documentation updates `DOC-001` through `DOC-003`.
- Resolution of the persisted `0xFE04` investigation in `DOC-005`.

## Next implementation slice

Introduce the setup-time resolved semantic model without changing operational
runtime, select-entity, persistence, or service behavior.

The checkpoint should:

1. Define an immutable resolved remote model containing:
   - the selected base profile;
   - the selected codeset definition when one is configured;
   - the deterministic ordered capability collection.
2. Resolve a manually configured remote from its stored `device_type`.
3. Resolve an imported codeset from its authoritative codeset binding.
4. Preserve the existing no-codeset and unknown-or-stale configuration behavior.
5. Define and test precedence when stored `device_type` and codeset metadata are
   both present.
6. Ensure codeset capabilities augment profile capabilities without duplicate
   resolved entries.
7. Add fake profile, codeset, and capability tests proving generic resolution.
8. Add invalid and missing-reference tests at the appropriate registry boundary.
9. Keep the resolved model independent of Home Assistant entity instances.
10. Preserve 100% coverage and every existing behavioral test.

Do not yet remove the Japanese tuner constants from `runtime.py` or migrate
`select.py`. First make setup-time profile, codeset, and capability resolution
independently testable and deterministic.

## New-chat handoff

To continue in a new chat, provide:

- Repository: `orandasoft/universal-remote`
- Branch: `feature/v0.7.0-protocol-profile-architecture`
- Read: `docs/v0.7.0-architecture-implementation-tracker.md`
- Baseline architecture document:
  `Universal Remote - Protocol and Profile Architecture - Revision 5`

The new chat should inspect the current branch and this tracker before
proposing or applying further changes.
